<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.PMDM_UF1_Jestify_2" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- prevents crashes -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- colors -->
        <item name="colorPrimary">@color/darker_teal</item>
        <item name="colorPrimaryVariant">@color/dim_teal</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/light_teal</item>
        <item name="colorSecondaryVariant">@color/darker_teal</item>
        <item name="colorOnSecondary">@color/light_teal</item>

        <!-- container colors -->
        <item name="colorPrimaryContainer">@color/darker_teal</item>
        <item name="colorOnPrimaryContainer">@color/dim_teal</item>

        <item name="colorSecondaryContainer">@color/darker_teal</item>
        <item name="colorOnSecondaryContainer">@color/dim_teal</item>

        <item name="colorTertiaryContainer">@color/dim_teal</item>
        <item name="colorOnTertiaryContainer">@color/light_teal</item>

        <!-- background colors -->
        <item name="android:colorBackground">@color/dim_grey</item>
        <item name="colorOnBackground">@color/grey</item>

        <!-- the small bar at top with the battery, wifi, etc. symbols -->
        <item name="android:statusBarColor">@color/darker_teal</item>
    </style>
</resources>